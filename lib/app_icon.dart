import 'package:flutter/material.dart';

class AppIcon extends Icon {
  const AppIcon(super.icon,
      {super.size = 22.0,
      super.color,
      super.semanticLabel,
      super.textDirection,
      super.key});
}
